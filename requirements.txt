# ── Core ────────────────────────────────────────────────────────────
torch>=2.1.0
torchvision>=0.16.0
transformers>=4.40.0
accelerate>=0.28.0
tokenizers>=0.19.0

# ── LLaVA-Med backbone ───────────────────────────────────────────────
sentencepiece>=0.1.99
einops>=0.7.0
peft>=0.10.0          # LoRA fine-tuning

# ── Module 1 — Meta-cognition ────────────────────────────────────────
scikit-learn>=1.4.0   # PCA + LogisticRegression probe
numpy>=1.26.0
scipy>=1.12.0

# ── Module 2 — Retrieval ─────────────────────────────────────────────
faiss-gpu>=1.7.4      # use faiss-cpu if no GPU available
open-clip-torch>=2.24.0   # BiomedCLIP
Pillow>=10.2.0

# ── Module 3 — DPO alignment ─────────────────────────────────────────
trl>=0.8.6            # DPO trainer
datasets>=2.18.0
bitsandbytes>=0.43.0  # 4-bit quantisation for training

# ── Evaluation ───────────────────────────────────────────────────────
nltk>=3.8.1
rouge-score>=0.1.2
sacrebleu>=2.4.0
evaluate>=0.4.1

# ── Interface ────────────────────────────────────────────────────────
gradio>=4.26.0

# ── Utilities ────────────────────────────────────────────────────────
pandas>=2.2.0
matplotlib>=3.8.0
seaborn>=0.13.0
tqdm>=4.66.0
pyyaml>=6.0.1
python-dotenv>=1.0.0
openai>=1.20.0        # GPT-4o for preference pair generation
loguru>=0.7.2
rich>=13.7.0          # pretty terminal output
